let edadUsuario = prompt ("Ingrese su edad");

function edad (edad){
    let edadUsuario;
    if (edad < 18){
        alert ("Usted esta ingresando bajo su responsabilidad");
    }
}
edad (edadUsuario);

